<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">MyMDB</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <a href="/movies/">Movies <span class="sr-only">(current)</span></a>
        </li>
        <li>
          <a href="/directors/">Directors</a>
        </li>
        <li>
          <a href="/actors/">Actors</a>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<a class="btn btn-default" href="/movies/new">Add movie</a>
<table>
  <tr>
    <th>Image</th>
    <th>Title</th>
    <th>Year</th>
    <th>Duration</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @movie.each do |movie| %>
  <tr>
    <td><img src="<%= movie.image_url %>")</td>
    <td><%= movie.title %></td>
    <td><%= movie.year %></td>
    <td><%= movie.duration %></td>
    <td><%= movie.description %></td>
    <td><a class="btn btn-default" href="/movies/<%= movie.id %>">Details</a></td>
    <td><a class="btn btn-default" href="/movies/<%= movie.id %>/edit">Edit</a></td>
    <td><a class="btn btn-default" href="/delete_movie/<%= movie.id %>">Delete</a></td>
  </tr>
  <% end %>
</table>
